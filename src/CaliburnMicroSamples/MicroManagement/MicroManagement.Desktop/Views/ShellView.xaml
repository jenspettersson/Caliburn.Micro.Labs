<Window x:Class="MicroManagement.Desktop.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Micro Management" 
        Height="600"
        Width="800"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <!--<ContentControl x:Name="StatusBar" DockPanel.Dock="Top" Height="30" />-->
        <Border Height="45" DockPanel.Dock="Top" Padding="5" Background="AliceBlue">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Micro " FontSize="24" FontStyle="Italic" />
                <TextBlock Text="Management" FontSize="24" />
            </StackPanel>
        </Border>

        <Border HorizontalAlignment="Stretch" DockPanel.Dock="Bottom" Padding="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Height="50">
                <Button x:Name="ShowManageEmployees" Content="Manage Employees" Margin="10" />
                
            </StackPanel>
        </Border>
        <Border Padding="5">
            <ContentControl x:Name="ActiveItem" />
        </Border>
    </DockPanel>
</Window>
